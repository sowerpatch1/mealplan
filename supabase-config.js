// Supabase project credentials — fill these in after creating your project.
// Get them from: https://app.supabase.com → Your Project → Settings → API
//
// The anon key is safe to commit to GitHub — Row Level Security protects your data.
// NEVER put the service role key here; it belongs only in your local SKILL.md.
const SUPABASE_URL     = "https://YOUR_PROJECT_REF.supabase.co";
const SUPABASE_ANON_KEY = "YOUR_ANON_KEY_HERE";
